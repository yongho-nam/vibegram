{
  "name": "instagram-clone",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "node scripts/install-backend.mjs && node scripts/dev.mjs",
    "build": "npm run build -w instagram-clone-frontend",
    "migrate": "node scripts/install-backend.mjs && node scripts/migrate.mjs"
  }
}
